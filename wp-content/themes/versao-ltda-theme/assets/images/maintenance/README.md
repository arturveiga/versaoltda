Pasta de imagens padrão do Modo Manutenção.

Arquivos padrão esperados (para fallback quando o admin não enviar uploads):
- default-logo.png
- default-background.jpg

Substitua pelos arquivos desejados.

